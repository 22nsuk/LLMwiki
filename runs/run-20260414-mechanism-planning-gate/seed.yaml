$schema: "ops/schemas/seed.schema.json"
run_id: run-20260414-mechanism-planning-gate
mode: improvement

request:
  summary: "Harden planning_gate_validate for system_mechanism run directories so promotion scaffolding mistakes fail early."
  requester: maintainer-agent
  source_pages:
    - "ops/README.md"
    - "runs/README.md"
    - "system/system-index.md"
    - "ops/templates/mechanism-run/seed.yaml"
    - "ops/templates/mechanism-run/promotion-report.json"

state:
  current: SEED_FROZEN
  allowed_next:
    - VALIDATION
    - BLOCKED

goals:
  primary:
    - "Teach planning_gate_validate to understand mechanism-run scaffolding better without widening it into a generic run orchestrator."
    - "Catch missing or malformed promotion scaffolding before mutation work starts."
    - "Leave behind the first clean baseline/candidate artifact layout for future mechanism_review history."
  non_goals:
    - "Do not implement full run_mechanism_experiment automation in this run."
    - "Do not change promotion_gate decision rules."
    - "Do not broaden the run beyond planning scaffold validation."

constraints:
  hard:
    - "One mechanism per experiment."
    - "Primary target stays ops/scripts/planning_gate_validate.py."
    - "Never edit raw/."
    - "Keep run-local artifacts under runs/run-20260414-mechanism-planning-gate/."
  soft:
    - "Prefer one focused helper or one narrow cross-check over a large CLI redesign."
    - "Prefer tests that freeze mechanism-run starter behavior over broad end-to-end orchestration."

success_criteria:
  - id: SC-001
    text: "planning_gate_validate recognizes mechanism-run promotion scaffolding strongly enough to fail early on missing or malformed promotion-report input."
    trace:
      - "ops/scripts/planning_gate_validate.py"
      - "tests/test_run_templates.py"
  - id: SC-002
    text: "The mechanism-run starter and the concrete run directory both stay schema-valid after the change."
    trace:
      - "ops/templates/mechanism-run/promotion-report.json"
      - "runs/run-20260414-mechanism-planning-gate/promotion-report.json"
  - id: SC-003
    text: "Repo health remains green and the run ends with a promotion report that explains the mechanism outcome."
    trace:
      - "runs/run-20260414-mechanism-planning-gate/promotion-report.json"
      - "runs/run-20260414-mechanism-planning-gate/run-ledger.json"

assumptions:
  open:
    - "Decide whether promotion-report validation should trigger whenever the file exists or only for mechanism-run starter bundles."
    - "Decide whether planning_gate_validate should only schema-check promotion-report.json or also cross-check its run_id against seed/run-ledger."
  frozen:
    - "The first run should target an existing mechanism file, not a brand-new wrapper script."
    - "Experiment success is read from runs/run-20260414-mechanism-planning-gate/promotion-report.json decision, not from shell exit code alone."

evidence:
  wiki_pages:
    - "system/system-index.md"
    - "system/system-log.md"
    - "system/query--recommended-next-actions-2026-04-12.md"
  raw_sources: []

signoff:
  status: pending
  by: ""
  ts: ""

notes: "Primary target: ops/scripts/planning_gate_validate.py. Expected supporting targets: tests/test_run_templates.py plus one narrow planning-gate regression test if needed. Focused test command should include planning_gate_validate on both ops/templates and this run directory."
