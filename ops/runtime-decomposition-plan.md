# Runtime Orientation

This file replaces the older decomposition note with a newcomer-friendly map of
the current maintainer runtime. It is not the full contract; it points to the
live owners and explains how the pieces fit together.

## Mental Model

The runtime has three cooperating layers:

1. Policy and schemas define allowed behavior and report shape.
2. Make targets orchestrate repeatable workflows.
3. Python packages implement bounded runtime steps and write schema-backed evidence.

Most work should start from a Make target, then follow the target to its script
package and schema.

## Main Runtime Areas

| Area | Package | Make file | What it owns |
| --- | --- | --- | --- |
| Shared runtime helpers | `ops/scripts/core/` | `mk/core.mk`, `mk/artifact.mk` | artifact IO, paths, policy, schemas, command execution, workflow planning |
| Eval and lint | `ops/scripts/eval/` | `mk/eval.mk`, `mk/test.mk` | wiki lint/eval, warning budget, complexity, doc audit |
| Registry | `ops/scripts/registry/` | `mk/registry.mk` | raw registry export, preflight, normalization, intake routing |
| Mechanism loop | `ops/scripts/mechanism/` | `mk/mechanism.mk` | mechanism review, mutation proposal, goal runtime, promotion experiments |
| Release | `ops/scripts/release/` | `mk/release.mk` | release evidence, external report lifecycle, sealing, closeout summaries |
| Learning | `ops/scripts/learning/` | `mk/release.mk` | learning claims, readiness signoff, remediation backlog, negative lessons |
| Public export | `ops/scripts/public/` | `mk/public.mk` | public mirror, public check, public-surface helpers |
| Supply chain | `ops/scripts/supply_chain/` | `mk/supply_chain.mk` | provenance, SBOM, advisory, OpenVEX, in-toto, Sigstore |

## How To Trace A Change

1. Identify the Make target that a user would run.
2. Read the recipe in `mk/*.mk`.
3. Follow the `python -m ops.scripts...` entrypoint to the canonical package.
4. Read the schema for any report the script writes.
5. Add or update tests near the runtime owner.
6. Close with the narrow Make/Pytest gate for that surface.

## Current Design Direction

- Keep orchestrators thin and move reusable state transitions into helper modules.
- Prefer schema-backed reports over prose-only evidence.
- Keep public export reproducible without private corpus state.
- Keep generated artifact promotion explicit: candidate under `tmp/`, validated durable report under policy-approved output.
- Keep compatibility aliases only as migration surfaces; new docs should teach current package paths.
- Keep authority axes explicit; do not collapse source-closeout, sealed-run, and diagnostic evidence into one synthetic status.
- Keep operator-facing currentness computed from live HEAD/fingerprint/domain checks, not from self-declared current fields alone.

## Responsibility Split Plan

The runtime-codehealth-hardening sprint uses the same façade shape that already
exists in `ops/scripts/mechanism/finalize_run.py`: the CLI module parses
arguments and maps known runtime exceptions to exit codes, while
`ops/scripts/mechanism/finalize_run_runtime.py` coordinates artifact loading,
state assembly, payload validation, and atomic writes through helper modules.
New hotspot work should move in that direction without changing public function
signatures or schema-backed report payloads.

Each hotspot is split along four boundaries:

| Boundary | Meaning | Extraction rule |
| --- | --- | --- |
| Loader | Read policy, reports, manifests, filesystem state, and injected runtime context. | Keep I/O here; return dataclasses or plain payloads that can be reused by tests. |
| Decision | Pure state transitions, ranking, gate decisions, and status classification. | No writes, no wall-clock calls, no schema output assembly. |
| Renderer | Assemble schema-backed reports from loaded inputs and decisions. | Use the injected `RuntimeContext` timestamp already carried by loader inputs. |
| Promotion rule | Decide whether an action may advance, stop, require operator review, or remain diagnostic. | Keep rules small, named, and testable with focused unit/property tests. |

### Planned Hotspot Splits

| Hotspot | Current façade | Loader extraction | Decision extraction | Renderer extraction | Promotion-rule extraction | First safe move |
| --- | --- | --- | --- | --- | --- | --- |
| `ops/scripts/mechanism/mutation_proposal_runtime.py` | `build_report()` now calls the loader, proposal assembly, diagnostics assembly, renderer helper, and validator in order. | Done: `ops/scripts/mechanism/mutation_proposal_loader_runtime.py` owns `MutationReportInputs`, policy overrides, current mechanism-review loading, optional report/session loading, and recent-log input selection. | Done for ordinary mechanism-review candidate assembly: `ops/scripts/mechanism/mutation_proposal_candidate_runtime.py` owns `MutationProposal`, `PriorityBreakdown`, recent-log match records, closed-remediation fallback, proposal IDs, metric/run-id selection, budget signal, blast-radius scoring, must-change target/test helpers, candidate-to-`MutationProposal` mapping, and neutral fixed-priority proposal breakdowns. Done for bootstrap proposal creation: `ops/scripts/mechanism/mutation_proposal_bootstrap_runtime.py` owns bootstrap/no-history proposal construction while the façade only appends the returned models. Done for recent-log-overlap queue-unblock proposal creation: `ops/scripts/mechanism/mutation_proposal_recent_log_overlap_runtime.py` owns policy gating, target rotation, outcome-history suppression, structural-budget blocking, and unblock proposal construction. Done for report queue selection and queue-pressure diagnostics: `ops/scripts/mechanism/mutation_proposal_queue_runtime.py` owns carry-forward repair prioritization, blocked/runnable queue diagnostics, and queue pressure summary rendering. Done for next-run repair queue glue: `ops/scripts/mechanism/_mutation_proposal_next_run_repair_runtime.py` owns dependency factory wiring, current-runnable proposal IDs, and next-run repair model creation without adding a public flat re-export alias. | Done for report payload/envelope assembly: `ops/scripts/mechanism/_mutation_proposal_render_runtime.py` owns the mutation report source paths, canonical envelope inputs, summary payload, and top-level render payload without adding a public flat re-export alias. | Done for status/blocker rules in `ops/scripts/mechanism/mutation_proposal_promotion_runtime.py`; keep moving remaining dedupe/recent-overlap promotion decisions only with focused tests. | Next safe move: leave mutation_proposal stable unless a concrete failing contract points at remaining façade orchestration. |
| `ops/scripts/mechanism/auto_improve_runtime.py` | `run_auto_improve_session()` coordinates request coercion, session start, loop state, iteration execution, completion, and maintenance. | Done for request/session start and goal-contract loading: `ops/scripts/mechanism/auto_improve_session_start_runtime.py` owns request coercion, new/resume session construction, goal-contract snapshot loading, budget validation, digest refresh compatibility, and start dataclasses. `ops/scripts/mechanism/auto_improve_value_runtime.py` owns shared scalar/dict/list coercion so maintenance code does not import from the start helper. Session report loading is done in `ops/scripts/mechanism/auto_improve_session_report_runtime.py`. | Done for loop stop decisions: `ops/scripts/mechanism/auto_improve_loop_decision_runtime.py` owns loop-state normalization, repeated-blocker/backlog gating, pre-run selected proposal metadata, and pre/post-iteration stop-reason checks. Done for maintenance action/cycle/eligibility decisions: `ops/scripts/mechanism/auto_improve_maintenance_decision_runtime.py` owns maintenance queue action metadata, interval/cycle coercion, eligibility, initial maintenance payload, terminal stop-condition mapping, and resume action planning. Done for maintenance completion rules: `ops/scripts/mechanism/_auto_improve_maintenance_completion_runtime.py` owns complete-payload assembly, time-budget stop-reason mapping, and loop stop-reason handoff without adding a public flat re-export alias. Done for learning-preflight authorization: `ops/scripts/mechanism/auto_improve_learning_preflight_runtime.py` owns goal-contract learning authorization, learning-mode payload decisions, blocked preflight messages, and the shared goal-run-admission authorization predicate while the façade keeps preflight I/O. Done for post-promotion maintenance stop-condition application: `ops/scripts/mechanism/auto_improve_promotion_stop_runtime.py` owns the final decision to skip, run maintenance, or rewrite the final stop reason after a promoted proposal exhausts proposal budget. | Done for session report rendering/loading/refresh: `ops/scripts/mechanism/auto_improve_session_report_runtime.py` owns generated-at normalization, envelope/source-path metadata, schema-backed writes, routing provenance side effect, session loading, and refresh summaries while the façade preserves compatibility imports. | Quarantine/pre-promotion failure classification is already owned by `ops/scripts/mechanism/auto_improve_outcome_runtime.py`; keep telemetry evidence assembly in iteration persistence. Remaining promotion/stop conditions should move into small rule helpers only with focused tests. | Next safe move: leave auto_improve stable until a concrete failing contract points at remaining façade orchestration. |
| `ops/scripts/mechanism/auto_improve_readiness_runtime.py` readiness cluster | `build_readiness_report()` loads reports, derives queue state, assesses execution/learning/promotion readiness, and renders the readiness report. | Done for report loading: `ops/scripts/mechanism/auto_improve_readiness_loader_runtime.py` owns disk/injected report loading, canonical artifact-envelope rejection, selected-contract currentness-only tolerance, release-authority preflight fallback selection, and required-report presence. Done for queue derivation ownership: `ops/scripts/mechanism/auto_improve_readiness_queue_runtime.py` owns `ReadinessQueueState`, and the façade now carries that object instead of duplicating queue scalar fields. | Done for `_dict_field` leakage, queue payload ownership, execution-readiness field ownership, promotion-readiness payload ownership, and learning claim blocker ownership: readiness queue/release-authority/worktree-guard helpers share `ops/scripts/core/payload_field_runtime.py::dict_field()`, queue/check/fallback/remediation assembly lives behind `readiness_queue_payloads()`, queue-derived execution reasons/next action live behind `readiness_execution_fields()`, promotion blocker summaries live behind `promotion_readiness_payload()`, and learning claim/signoff suppression lives behind `learning_claim_blocker_payloads()`. Next safe decision move is summary preparation only if focused readiness byte-stability tests show churn. | Done for readiness `file_inputs`, `inputs`, and `diagnostics` payload ownership: `ops/scripts/mechanism/auto_improve_readiness_payload_runtime.py` owns those schema sections while the façade keeps the top-level envelope/report assembly. | Keep admission independent: `goal_runtime_run_admission.py` must recompute runnable queue/currentness instead of trusting readiness' snapshot. | Next safe move: leave report loading stable; split summary preparation only after a concrete churn/failure signal points at it. |
| `ops/scripts/release/release_evidence_dashboard.py` | `build_report()` loads dashboard reports, derives signals, gates, status counts, then calls `_render_dashboard_report()`. | Keep `_load_dashboard_reports()` as the seed and move report loading plus fingerprint/currentness input gathering into a dashboard input helper. | Done for fixed-point finalizer duration/cost signal: `ops/scripts/release/release_evidence_dashboard_finalizer_runtime.py` owns fixed-point/cost-trend digests, basis relation, threshold status, writer-cost payloads, and finalizer budget signal assembly. Done for learning-delta guard signal/gate: `ops/scripts/release/release_evidence_dashboard_learning_delta_runtime.py` owns scoreboard parsing, missing-input fallback, confirmed evidence summary, claim model derivation, coverage status, unlock-review-only attention, and learning guard gate rendering. Done for closeout input/decision gate: `ops/scripts/release/release_evidence_dashboard_closeout_runtime.py` owns release-status-v2 fallback interpretation, machine/operator release allowance, accepted-risk review attention, and closeout gate rendering. Move component gates, status counts, and dashboard status classification into decision helpers. | Done for renderer: `ops/scripts/release/release_evidence_dashboard_render_runtime.py` owns dashboard input payloads, summary payloads, budget signals, accepted-risk delta rendering, and source-path metadata for split helpers. | Move advisory lifecycle and signoff revalidation into promotion-rule helpers. | Next safe move: extract status-count/dashboard-status aggregation or component gate helpers after closeout helper tests, source-path assertions, and façade golden stay current. |
| `ops/scripts/release/release_closeout_summary.py` | `build_report()` already calls `_load_closeout_sources()`, `_prepare_closeout_state()`, and `_render_closeout_report()` through existing source/risk/render helper modules. | Continue moving source specs, source loading, learning signoff loading, previous closeout loading, and dependency reproducibility input preparation into `release_closeout_source_runtime.py` or a closeout input helper. | Move component evaluation, source-tree coherence, live make check, readiness state, and downstream digest mismatch decisions into closeout decision helpers. | Keep report assembly in `release_closeout_render_runtime.py`; move remaining envelope/file-input assembly there only after schema samples and closeout tests are pinned. | Keep accepted-risk finalization, taxonomy coverage, signoff application, and clean-lane blocking decisions in `release_closeout_risk_runtime.py` or smaller promotion-rule helpers. | Finish the partially-complete split by moving the remaining envelope/input glue out of the large module while keeping `build_report()` as the stable façade. |
| `ops/scripts/release/external_report_lifecycle_runtime.py` | Lifecycle helper module consumed by `external_report_action_matrix.build_report()` for inventory, profile, lifecycle, and release-verification evidence through sub-runtime imports plus in-module reason-target tables. | Move inventory/profile loading and report-text ingestion into dedicated loader helpers while keeping sub-runtime imports as the first seam. | Move action-status resolver tables, lifecycle assembly, and archive-reconciliation reason mapping into decision helpers. | Move action-matrix lifecycle envelope/source-path assembly only from the action-matrix façade that owns report rendering. | Keep release-verified action promotion rules in `external_report_release_verification_runtime.py` and related verification helpers. | Next safe move: correct inventory/profile loader ownership before touching reason-target tables; keep action-matrix report rendering in its current façade. |
| `ops/scripts/release/release_closeout_gate_runtime.py` | `closeout_gates()` and `collect_closeout_components()` coordinate source loading, gate assembly, risk state, and readiness classification for closeout summary. | Keep source loading, component collection, and artifact-freshness path resolution in the gate façade. | Done for readiness/test-lane/smoke/live-make gate classification: `ops/scripts/release/release_closeout_gate_decision_runtime.py` owns release readiness states, test-failure lane parsing, boundedness/live-make gate records, and closeout readiness assembly. Move component evaluation and source-tree coherence decisions next. | Keep report assembly in `release_closeout_render_runtime.py`. | Keep accepted-risk finalization and taxonomy coverage in `release_closeout_risk_runtime.py`. | Next safe move: extract `evaluate_*_source` component decisions or `source_tree_coherence` after decision-helper tests and closeout summary tests stay green. |
| `ops/scripts/release/release_closeout_batch_manifest.py` | `build_batch_manifest()` / `main()` coordinate ZIP inventory, digest checks, release decision assembly, and envelope rendering in one module. | Done for ZIP/archive metadata: `ops/scripts/release/release_closeout_batch_manifest_zip_runtime.py` owns ZIP path normalization, archive manifest construction, member mtime extraction, and timestamp semantics. Next loader move is artifact inventory and upstream report loading into a batch-manifest loader helper. | Extract digest mismatch, status classification, and release decision dataclasses into decision helpers. | Extract envelope/report assembly into a renderer helper and keep CLI in a thin façade. | Keep promotion/finality blocking rules with existing closeout authority helpers. | Next safe move: extract artifact inventory loading after ZIP helper tests and batch manifest focused tests stay green. |
| `ops/scripts/mechanism/goal_runtime_run_admission.py` | `build_report()` plus CLI entrypoint coordinate 12+ upstream report loads and gate decisions. | Done for shared JSON loading through `goal_runtime_json_loader_runtime.py` across admission, closeout, quarantine preflight, stale closeout, fixed-point, and clean-transient modules. Next loader move is upstream admission report loading into an admission loader helper. | Extract gate classification and runnable-queue recomputation into decision helpers independent of readiness snapshots. | Extract admission report envelope assembly into a renderer helper. | Keep promotion/admission blocking rules aligned with goal-runtime certificate semantics. | Next safe move: extract `_load_admission_reports()` after JSON-loader guard tests and admission/hotspot golden checks stay current. |

### Completed First-Split Moves

The first extraction pass intentionally moved only seams that were already
pure or already sat at a stable report assembly boundary.

| Hotspot | Extracted helper | Boundary | Contract guard |
| --- | --- | --- | --- |
| `ops/scripts/core/command_log_summary_backfill.py` | `_BackfillState` plus group collection, summary-reference repair, closure, fingerprint, raw-delete, and summary helpers. | Loader/decision/renderer: legacy command-log group handling and backfill report assembly while deleting the unused local reference accumulator. | Command-log summary backfill focused tests plus static checks. |
| `ops/scripts/core/command_runtime.py` | `RunWithTimeoutRequest`, `_RunWithTimeoutState`, and completed/timeout result helpers. | Loader/decision/renderer: timeout execution request coercion, process start state, communicate handling, timeout recovery, and result payload assembly are split while legacy `run_with_timeout(argv, cwd=..., timeout_seconds=...)` callers remain supported. | Command runtime, heartbeat, subprocess smoke tests plus static checks. |
| `ops/scripts/core/request_coercion_runtime.py` | `coerce_request_or_kwargs()`, first used by next-run decision and release-source-ready commit helpers. | Cross-cutting hygiene: pure request-object-or-legacy-kwargs coercion is shared without absorbing positional, dict, allowlist, or path-resolving compatibility contracts. | Request coercion helper tests plus next-run decision and release source ready commit focused tests. |
| `ops/scripts/core/payload_field_runtime.py` | `dict_field()` now serves readiness queue, release-authority, worktree-guard, and façade summary helpers. | Cross-cutting hygiene: nested dict payload access is shared inside the readiness cluster instead of importing private helpers across ownership boundaries. | Payload helper tests plus readiness queue/runtime focused tests and static checks. |
| `ops/scripts/core/codex_goal_client.py` | Section builder helpers in the same module. | Renderer: goal contract payload sections for guard, roots, budgets, execution policy, backend, stop conditions, evidence, and metadata. | `tests/test_codex_goal_client.py`, goal runtime certificate tests, and goal runtime runner tests. |
| `ops/scripts/mechanism/goal_runtime_certificate_report.py` | State/render helpers in the same module. | Loader/renderer: certificate update state, file-input collection, command observability, envelope assembly, and final report rendering. | Goal runtime certificate, admission, and status tests. |
| `ops/scripts/mechanism/mutation_proposal_runtime.py` | `ops/scripts/mechanism/mutation_proposal_loader_runtime.py` | Loader: report input dataclass, policy override handling, current mechanism-review validation, optional outcome/remediation/session report loading, and recent-log selection. | Focused build-report tests plus the façade golden digest. |
| `ops/scripts/mechanism/mutation_proposal_runtime.py` | `ops/scripts/mechanism/mutation_proposal_promotion_runtime.py` | Promotion rule: source-evidence gaps, empty-queue blockers, blocked count, report status. | Focused helper tests plus the façade golden digest. |
| `ops/scripts/mechanism/mutation_proposal_runtime.py` | `ops/scripts/mechanism/mutation_proposal_candidate_runtime.py` | Decision: ordinary mechanism-review candidate-to-proposal mapping, proposal model dataclasses, recent-log match records, closed-remediation fallback, must-change target/test helpers, proposal identity, metric/run-id selection, budget signal, blast-radius scoring, and generic why-now text. | Mutation proposal build/promotion/recent-overlap focused tests, runtime hotspot golden digest, and static checks. |
| `ops/scripts/mechanism/mutation_proposal_runtime.py` | `ops/scripts/mechanism/mutation_proposal_bootstrap_runtime.py` | Decision: bootstrap/no-history mutation proposal construction, bootstrap priority, bootstrap budget signals, fallback target/test resolution, and bootstrap proposal source identity. | Bootstrap/no-history mutation proposal focused tests, report-generation smoke, runtime hotspot golden digest, and static checks. |
| `ops/scripts/mechanism/mutation_proposal_runtime.py` | `ops/scripts/mechanism/mutation_proposal_recent_log_overlap_runtime.py` | Decision: all-queue recent-log-overlap unblock gating, preferred/discovered target rotation, structural-complexity and recent-outcome blockers, promotion-history resolution, and queue-unblock proposal construction. | Recent-log-overlap focused tests, mutation build-report focused tests, runtime hotspot golden digest, schema sample check, and static checks. |
| `ops/scripts/mechanism/mutation_proposal_runtime.py` | `ops/scripts/mechanism/mutation_proposal_queue_runtime.py` | Decision: report queue selection, carry-forward repair prioritization, blocked/runnable queue diagnostics, and queue pressure summary text. | Queue helper focused tests, mutation build/promotion/recent-overlap focused tests, runtime hotspot golden digest, and static checks. |
| `ops/scripts/mechanism/next_run_repair_queue_runtime.py` | `_NextRunRepairScope` and `_next_run_repair_scope()`. | Loader/decision: primary/supporting targets, test targets, source run identity, failure taxonomy, and evidence paths for repair proposals. | Next-run repair queue tests plus mutation proposal build/promotion tests. |
| `ops/scripts/mechanism/auto_improve_runtime.py` | `ops/scripts/mechanism/auto_improve_maintenance_decision_runtime.py` | Decision/promotion rule: maintenance queue action, cycle metadata, runtime maintenance eligibility, and terminal maintenance stop reason. | Focused helper tests plus selected auto-improve session/admission tests and runtime hotspot smoke. |
| `ops/scripts/mechanism/auto_improve_runtime.py` | `ops/scripts/mechanism/auto_improve_loop_decision_runtime.py` | Decision: loop-state reconstruction/normalization, repeated-blocker and repeat-backlog stop gates, pre-run repeat-repair metadata, initial loop state, and pre/post-iteration stop-reason checks. | Loop decision focused tests plus selected auto-improve runtime, maintenance, iteration, goal, and schema checks. |
| `ops/scripts/mechanism/auto_improve_runtime.py` | `ops/scripts/mechanism/auto_improve_session_report_runtime.py` | Loader/renderer: auto-improve session report loading, generated-at normalization from next-run decisions, envelope/source-path metadata, schema-backed persistence, routing provenance write-through, and refresh summaries. | Session report writer/refresh focused tests plus auto-improve bundle golden, schema sample, and static checks. |
| `ops/scripts/mechanism/auto_improve_runtime.py` | `ops/scripts/mechanism/auto_improve_session_start_runtime.py`, `ops/scripts/mechanism/auto_improve_value_runtime.py`, and `ops/scripts/mechanism/auto_improve_error_runtime.py` | Loader: request-object/legacy-kwargs coercion, new/resume session construction, goal-contract snapshot loading, budget validation, compatible digest refresh, shared scalar/dict/list coercion, and shared error types while the façade preserves compatibility imports. | Session-start helper tests, selected auto-improve runtime goal-contract/resume tests, CLI goal-contract path test, auto-improve bundle golden, schema sample, and static checks. |
| `ops/scripts/mechanism/auto_improve_runtime.py` / `ops/scripts/mechanism/goal_runtime_run_admission.py` | `ops/scripts/mechanism/auto_improve_learning_preflight_runtime.py` | Promotion rule: learning-uncertain bounded trial authorization, learning-mode payload decisions, blocked preflight messages, and shared goal contract authorization semantics. | Learning preflight helper tests, selected auto-improve runtime learning tests, goal runtime run admission learning tests, auto-improve bundle golden, schema sample, and static checks. |
| `ops/scripts/mechanism/auto_improve_runtime.py` | `ops/scripts/mechanism/auto_improve_promotion_stop_runtime.py` | Promotion/stop rule: post-promotion proposal-budget exhaustion now resolves through a focused helper that decides whether to skip maintenance, run maintenance, or rewrite the final stop reason to time-budget exhaustion. | Promotion-stop helper tests, session report source-path tests, selected auto-improve runtime maintenance tests, auto-improve bundle golden, and static checks. |
| `ops/scripts/mechanism/auto_improve_readiness_runtime.py` | `ReadinessQueueState`, `readiness_queue_payloads()`, `readiness_execution_fields()`, `promotion_readiness_payload()`, and `learning_claim_blocker_payloads()` are now carried/used by `ReadinessInputs` and `render_readiness_report()`. | Loader/decision/renderer ownership: queue derivation, queue payload, fallback/check/remediation assembly, execution field assembly, promotion readiness summary, and learning claim/signoff suppression stay in their owner modules while the façade no longer unpacks and re-owns those scalar fields. | Readiness runtime, readiness queue, release-authority, queue selection, goal admission, next-run repair, schema sample, report-contract core, and static checks. |
| `ops/scripts/mechanism/auto_improve_readiness_runtime.py` | `ops/scripts/mechanism/auto_improve_readiness_loader_runtime.py` | Loader: disk and injected report payload loading, canonical report-envelope filtering, selected-contract currentness-only tolerance, release-authority preflight fallback selection, and required-report presence calculation. | Readiness loader focused tests, readiness runtime byte-stability/source-path tests, auto-improve readiness runtime tests, and static checks. |
| `ops/scripts/mechanism/auto_improve_readiness_runtime.py` | `ops/scripts/mechanism/auto_improve_readiness_payload_runtime.py` | Renderer/input glue: readiness report `file_inputs`, `inputs`, and `diagnostics` schema sections are owned by a payload helper while the façade keeps top-level envelope and blocker assembly. | Readiness runtime byte-stability/source-path tests plus static checks. |
| `ops/scripts/release/release_evidence_dashboard.py` | `ops/scripts/release/release_evidence_dashboard_render_runtime.py` | Renderer: dashboard input payloads, summary payloads, budget signals, accepted-risk delta rendering. | Dashboard focused report-contract test plus façade golden digest. |
| `ops/scripts/release/release_evidence_dashboard.py` | `ops/scripts/release/release_evidence_dashboard_finalizer_runtime.py` | Decision/input glue: fixed-point finalizer cost/duration signal, current digest calculation, cost-trend basis relation, threshold attention, writer-cost payloads, and missing-evidence fallback. | Finalizer helper tests, selected dashboard report tests, dashboard source-path assertion, runtime hotspot smoke, schema/surface checks, and façade golden digest. |
| `ops/scripts/release/release_evidence_dashboard.py` | `ops/scripts/release/release_evidence_dashboard_learning_delta_runtime.py` | Decision/input glue: learning-delta scoreboard guard summary, missing-input fallback, confirmed evidence and claim-model derivation, coverage status, unlock-review-only attention, and learning guard gate rendering. | Learning-delta helper tests, selected dashboard learning gate tests, dashboard source-path assertion, runtime hotspot smoke, schema/surface checks, and façade golden digest. |
| `ops/scripts/release/release_evidence_dashboard.py` | `ops/scripts/release/release_evidence_dashboard_closeout_runtime.py` | Decision/input glue: release closeout status-v2 fallback interpretation, machine/operator release allowance, accepted-risk review attention, missing closeout fallback, and release closeout decision gate rendering. | Closeout helper tests, selected dashboard closeout tests, dashboard source-path assertion, runtime hotspot smoke, schema/surface checks, and façade golden digest. |
| `ops/scripts/release/release_closeout_summary.py` | `ops/scripts/release/release_closeout_envelope_runtime.py` | Renderer/input glue: closeout envelope constants and file-input source identity assembly. | Closeout focused report-contract tests plus façade golden digest. |
| `ops/scripts/release/release_closeout_summary.py` | `ops/scripts/release/release_closeout_render_runtime.py` | Renderer/input glue: `CloseoutEnvelopeInputs`, `closeout_file_inputs()`, and `closeout_envelope()` now live in the render helper while loader/state assembly stays in envelope_runtime. | Closeout focused report-contract tests, runtime hotspot golden digest, and static checks. |
| `ops/scripts/release/release_evidence_dashboard.py` | `ops/scripts/release/release_evidence_dashboard_status_runtime.py` | Decision: component gate assembly, signoff/test-lane gate helpers, and dashboard status/count aggregation extracted from the façade. | Dashboard focused report-contract tests, runtime hotspot golden digest, and static checks. |
| `ops/scripts/release/external_report_lifecycle_runtime.py` | Reason-detail resolver tables and helpers in the same module. | Decision: action-status reason mapping, prefix handling, and exact release auto-promotion reason IDs. | External report action matrix status/lifecycle/static tests. |
| `ops/scripts/release/external_report_release_verification_runtime.py` | Release verified reason-id helper sets in the same module. | Decision: verified action reason ID assembly while preserving reason order. | External report action matrix status/lifecycle/static tests. |
| `ops/scripts/release/release_closeout_batch_manifest.py` | `ops/scripts/release/release_closeout_batch_manifest_zip_runtime.py` | Loader/input glue: ZIP member path normalization, archive manifest construction, member mtime extraction, and timestamp semantics for source freshness and distribution package checks. | Batch manifest ZIP helper tests plus release closeout batch manifest focused tests. |
| `ops/scripts/release/release_closeout_gate_runtime.py` | `ops/scripts/release/release_closeout_gate_decision_runtime.py` | Decision: release readiness states, test-failure lane parsing, boundedness/live-make gate records, and closeout readiness assembly while the façade keeps source loading and component collection. | `tests/test_release_closeout_gate_decision_runtime.py`, release closeout summary tests, and static checks. |
| `ops/scripts/release/release_source_ready_commit.py` | `RunCommitRequest`, `_RunCommitState`, and head/amend/dirty-path/stage/commit guard helpers. | Loader/decision/renderer: release-source-ready commit request coercion, snapshot/amend validation, dirty path policy, dry-run/no-change handling, and git side effects are separated while legacy CLI keyword calls remain supported. | Release source ready commit focused tests plus static checks. |
| `ops/scripts/release/release_status_surface.py` | `StatusSurfaceSignals` plus status-line builder helpers. | Renderer: release status signal coercion, lock/dependency detail rendering, and each public status line are split while legacy keyword callers remain supported. | Release status surface focused tests plus static checks. |
| `ops/scripts/release/release_workflow_order_guard.py` | `_WorkflowOrderInputs`, check assembly, envelope, summary, and target-recipe helpers. | Loader/renderer: Makefile invocation loading, planner report reuse, guard check assembly, and report payload rendering. | Release workflow order guard tests plus static checks. |
| `tests/test_release_workflow_order_guard.py` | Scenario recipe constants, Makefile template helper, and a thin `_write_makefile()` fixture method. | Test-lane cost/readability: workflow-order scenario inputs are table-like constants instead of a 200-line fixture method. | Release workflow order guard focused tests plus static checks. |
| `tests/test_makefile_auto_improve_goal_static_gates.py` | Purpose-specific goal admission, contract/status, lock/preflight, local evidence, closeout, and refresh recipe tests. | Test-lane cost/readability: auto-improve goal Makefile assertions are split by runtime phase instead of two large static gate methods. | Auto-improve goal Makefile focused tests plus static checks. |
| `tests/test_makefile_test_execution_summary_gates.py` | Phony target and assignment tables plus purpose-specific recipe tests. | Test-lane cost/readability: report-contract, currentness/preflight, and full-suite reuse assertions are split by scenario instead of one large Makefile static gate. | Test execution summary Makefile focused tests plus static checks. |
| `tests/test_external_report_action_matrix_status.py` | Goal-native fixture constants, report builders, and separate active/completed contract assertions. | Test-lane cost/readability: goal runtime external-report action evidence is prepared through named synthetic report builders instead of one long action-matrix scenario method. | External report action matrix status focused tests plus static checks. |
| `tests/test_run_mechanism_experiment.py` / `tests/run_mechanism_experiment_test_utils.py` | Raw-registry preflight convergence constants, fake callback builders, run helper, and artifact assertion helpers now live in the wrapper fixture utility. | Test-lane cost/readability: wrapper-driven generated artifact convergence is owned by the shared scenario harness while the test method stays focused on setup and assertions. | Run mechanism experiment focused tests plus static checks. |
| `ops/scripts/release/release_auto_promotion_ready.py` | `_ReadyCheckInputs` plus phase, goal-runtime, manifest-authority, operator, and auto-improve check helpers. | Decision/renderer: ready-check assembly is grouped by authority area before manifest rendering. | Release auto-promotion ready focused tests plus static checks. |
| `ops/scripts/mechanism/auto_improve_next_run_decision_runtime.py` | `NextRunDecisionRequest`, request coercion, executor-report path, evidence path, and decision-builder helpers. | Loader/decision: next-run decision inputs are carried by a typed request while legacy keyword callers remain supported by the facade. | Next-run decision tests, next-run repair queue tests, selected auto-improve iteration tests, and static checks. |
| `ops/scripts/test/test_execution_reuse_runtime.py` | `ReuseCurrentnessRequest`, command identity helpers, and diagnostics assembly helpers. | Loader/decision: reuse currentness diagnostics are request-shaped, with existing dictionary and legacy keyword call surfaces preserved. | Test execution summary focused tests plus static checks. |
| `ops/scripts/mechanism/goal_runtime_run_admission.py` and goal-runtime cluster | `goal_runtime_json_loader_runtime.py` | Loader/cross-cutting hygiene: shared vault/path JSON loading for admission, closeout, preflight, stale closeout, fixed-point, and clean-transient modules before admission loader extraction. | Goal runtime JSON loader focused tests plus existing goal-runtime admission/closeout tests. |

No compatibility shim or duplicate implementation was kept for these moves:
the original blocks were deleted from the hotspot modules in the same change.

### Complexity Ratchet Ceiling

`system_refactor_policy.complexity_ratchet` stores the structural complexity
ratchet as policy, not as a report-schema extension. The contract is split into
two ledgers so the gate can block both new debt and resurfaced debt.

- `warn_targets`: active ceiling entries currently allowed to remain in `warn`.
- `resolved_targets`: former ceiling entries that returned to `pass` and must
  now fail immediately if they reappear as `warn`.

`complexity-budget-touched-check` stays a touched-surface gate. It still writes
the same touched complexity report, then applies policy-backed ratchet
regression logic when touched inputs are provided. This keeps the existing
structural complexity report schema unchanged while making new warn targets and
resurfaced debt actionable.

### Pre-Split Golden Baseline

`tests/test_runtime_hotspot_facade_golden_outputs.py` pins a slow, synthetic
baseline for the four first-split façades. It builds each output twice with a
fixed injected clock, validates the schema-backed payload, scans for local path
leaks, and compares canonical JSON bytes against frozen digests. The
auto-improve façade is intentionally captured as a normalized artifact bundle
because it writes several sidecars; raw file hashes that vary with in-memory
dict ordering are converted to canonical loaded-payload hashes before digesting.
Run this test before and after any extraction that touches the planned hotspot
files.

## Preservation And Delete-First Ledger

Use this ledger whenever a split keeps code that appears redundant or
temporary. If a preserved item is not listed here with a reason, the default is
to delete it during the extraction that made it unnecessary.

| Item | Status | Reason |
| --- | --- | --- |
| Existing public entrypoints such as `build_report()`, `write_report()`, `parse_args()`, and `main()` | Preserve | Tests, Make targets, compatibility aliases, and report-contract smoke tests import these names directly. They should become thin façades, not disappear during helper extraction. |
| Existing schema-backed report fields and envelope source paths | Preserve until an intentional contract update | Downstream report consumers depend on byte-identical field shape. Helper extraction can add helper source paths only with matching schema/sample/test/docs updates. |
| Large pure-ish helper blocks inside hotspot files | Temporary preserve | Planning alone does not prove dead code. Move them only with focused tests, then delete the original block in the same change. |

No dead or duplicate implementation is intentionally preserved by this plan.
Future extraction tasks must update this ledger before leaving any redundant
compatibility wrapper, shim, or unused helper behind.

### P2 Deferral Ledger

Use this ledger when a function-budget proposal is real but the only safe fix
would require an intentional public façade migration rather than a private
helper extraction.

No current P2 function-budget deferral is intentionally retained. New entries
must name the public façade contract, the verification that prevents immediate
removal, and the condition that makes the migration safe.

## Entry Documents

- Repository overview: [../README.md](../README.md)
- Architecture: [../ARCHITECTURE.md](../ARCHITECTURE.md)
- Ops runtime map: [../docs/ops-runtime.md](../docs/ops-runtime.md)
- Self-improvement loop: [../docs/self-improvement-runtime.md](../docs/self-improvement-runtime.md)
- Development checks: [../docs/development.md](../docs/development.md)
